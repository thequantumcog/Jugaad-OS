# Jugaad-OS
A minimal, humor-driven OS for tinkering

## ISO-Library Versions
* Linux 6.18.5
* wlroots 0.19.2
* libudev-zero commit: bbeb7ad
* toybox commit: 22efb79
* seatd commit: a3c56c3

## Installed-Programs
* dosbox
* foot
* vi
* bash

## Desi-Enhancements

| Instead of     | Use                         | What it does                                   |
| -------------- | --------------------------- | ---------------------------------------------- |
| `cd`           | `jaoo`                      | Changes directory with unsolicited life advice |
| `ls`           | `dekho`                     | Lists files after judging your curiosity       |
| `rm`           | `mitao`                     | Deletes files with irreversible warnings       |
| `cat`          | `billi`                     | Reads files (billi ne parh liya)               |
| `grep`         | `dhundo`                    | Searches files with divine motivation          |
| `ping`         | `zinda_hai`                 | Checks if a host is still breathing            |
| `chmod`        | `aukaat`                    | Changes permissions, reminds you of limits     |
| `top`          | `kya_ho_raha_hai`           | Shows system chaos                             |
| `kill / pkill` | `bnda_maro / goli_maro`     | Ends processes violently                       |
| `mkdir`        | `ghar_banao`                | Creates directories (qabza complete)           |
| `mv`           | `pehnko`                    | Moves files, then denies responsibility        |
| `cp`           | `naqal_kro`                 | Copies files (aqal required)                   |
| `pwd`          | `kahan_hoon`                | Location reminder                              |
| `whoami`       | `mein_kon`                  | Identity crisis resolver                       |
| `df`           | `jagah`                     | Disk space check                               |
| `clear`        | `saaf`                      | Clears terminal and evidence                   |
| `touch`        | `chhu`                      | Creates files via kala jaadu                   |
| `date`         | `tareekh`                   | Displays current suffering                     |
| `history`      | `karnamay`                  | Shows your past sins                           |
| `sleep`        | `sabar`                     | Waits patiently                                |
| `tar`          | `potli_banao / potli_kholo` | Packs / unpacks baggage                        |
| `sudo`         | `abbaji`                    | No sudo in actuality just a wrapper            |
| `exit`         | `khuda_hafiz`               | Leaves with dignity                            |

## Command Shaming (Aliases)
Standard commands like ls, rm, sudo, etc. are aliased to politely insult you and tell you the correct Jugaad-OS way to do things.
* Example:
```(bash)
$ cp
Aqal lagao! NAQAL_KRO type karo!
```

